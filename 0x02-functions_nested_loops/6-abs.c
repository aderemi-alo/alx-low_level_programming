#include "main.h"

/**
 * _abs - Function that prints absolute value of integer
 *
 * @n: Takes iput of type integer for the function
 *
 * Return: Always 0 (success)
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
